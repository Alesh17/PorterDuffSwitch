/* Для обновления нужно: поменять версию в этом файле, сделать новый релиз на GitHub, зайти на JitPack и проверить, все ли нормально собралось. Done! */

buildscript {

    ext.app_version = '2.8.1'
    ext.kotlin_version = '1.6.21'

    repositories {
        google()
        jcenter()
    }

    dependencies {
        classpath 'com.android.tools.build:gradle:7.2.0'
        classpath "org.jetbrains.kotlin:kotlin-gradle-plugin:$kotlin_version"
    }
}

allprojects {
    repositories {
        google()
        jcenter()
        maven { url "https://jitpack.io" }
    }
}

task clean(type: Delete) {
    delete rootProject.buildDir
}